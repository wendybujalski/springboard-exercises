// Map Callback
const doubleFunction = arr => arr.map(val => val * 2);

// All Arrow Functions
const squareAndFindEvens = numbers => numbers.map(num => num ** 2).filter(square => square % 2 === 0);